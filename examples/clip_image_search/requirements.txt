# Extra dependencies for the CLIP image-search example.
# Install with:  uv pip install -r examples/clip_image_search/requirements.txt
#
# torch is large. For CPU-only machines the CPU wheel is much smaller:
#   uv pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
transformers>=4.40
pillow>=10.0
streamlit>=1.30
